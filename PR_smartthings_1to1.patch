diff --git a/index.html b/index.html
index 1a2b3c4..7f8e9d0 100644
--- a/index.html
+++ b/index.html
@@ -1,16 +1,20 @@
 <!doctype html>
 <html lang="it">
 <head>
   <meta charset="utf-8"/>
   <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
   <title>Automazione • Home</title>
-  assets/style.css
+  <!-- No-store per evitare caching aggressiva dell'HTML -->
+  <meta http-equiv="Cache-Control" content="no-store, no-cache, must-revalidate, max-age=0"/>
+  <meta http-equiv="Pragma" content="no-cache"/>
+  <meta http-equiv="Expires" content="0"/>
+  <!-- CSS con cache-busting -->
+  <link rel="stylesheet" href="assets/style.css?v=20260222T1420">
 </head>
 <body>
   <div id="app">
     <!-- Top bar -->
     <header class="topbar">
       <div class="title-area">
         <h1 class="app-title">Home</h1>
         <span id="stateBadge" class="state-badge">—</span>
@@ -101,7 +105,8 @@
     </nav>
   </div>
 
   <div id="toast" class="toast"></div>
-  js/app.js</script>
+  <!-- JS con cache-busting -->
+  <script src="js/app.js?v=20260222T1420"></script>
 </body>
 </html>
diff --git a/js/app.js b/js/app.js
index 51ae594..aaaa111 100644
--- a/js/app.js
+++ b/js/app.js
@@ -1,6 +1,11 @@
 /* Automazione UI — SmartThings-like 2x2 */
-/* Config: ENDPOINT_URL è già impostato sul tuo Apps Script /exec */
+/* Config: ENDPOINT_URL è già impostato sul tuo Apps Script /exec */
 const ENDPOINT_URL = 'https://script.google.com/macros/s/AKfycbzRf9E4gTCHnq3-9Hm6BPLjIxwuTM5CiawvdbV1zKFfVQew0MWMD-Eq8nTqRW_c67uG/exec';
+
+// Hard no-cache sugli script JSONP (aggiunge un t=timestamp alla richiesta)
+function nocache(url){
+  const sep = url.includes('?') ? '&' : '?'; return url + sep + 't=' + Date.now();
+}
 
 /* Stato UI */
 let MODEL = null;
@@ -62,7 +67,7 @@ function jsonp(path, cbname){
   return new Promise((resolve,reject)=>{
     const cb = cbname || ('cb_'+Math.random().toString(36).slice(2));
     window[cb] = (res)=>{ try{ delete window[cb]; resolve(res); }catch{} };
-    const url = `${ENDPOINT_URL}${path}${path.includes('?')?'&':'?'}callback=${cb}`;
+    const url = nocache(`${ENDPOINT_URL}${path}${path.includes('?')?'&':'?'}callback=${cb}`);
     const s = document.createElement('script'); s.src = url; s.onerror = reject;
     document.body.appendChild(s);
     setTimeout(()=>{ try{ s.remove(); }catch{} }, 3000);
@@ -88,7 +93,7 @@ function loadModel(){
   return new Promise((resolve,reject)=>{
     window.onModel = (m)=>{ try{ MODEL=m; renderHome(m); resolve(m); }catch(e){reject(e);} };
     const s = document.createElement('script');
-    s.src = `${ENDPOINT_URL}?callback=onModel`; s.onerror = reject;
+    s.src = nocache(`${ENDPOINT_URL}?callback=onModel`); s.onerror = reject;
     document.body.appendChild(s);
     setTimeout(()=>{ try{ s.remove(); }catch{} }, 3000);
   });